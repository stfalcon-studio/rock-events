<div id="tab-manager-4" class="scroll">
    {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_rest(form) }}
        <button type="submit" class="profile-form__submit" style="display: block; margin-top: 20px;">Створити гурт</button>
    {{ form_end(form) }}
</div>
