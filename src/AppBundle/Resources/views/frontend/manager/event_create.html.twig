<div id="tab-manager-1" class="scroll">
    {{ form_start(form) }}
        {{ form_errors(form) }}

        {{ form_rest(form) }}
        {% include ':form:group.html.twig' with { action : 'event_groups' } %}
        <button type="submit" class="profile-form__submit" style="display: block">Створити подію</button>
    {{ form_end(form) }}
</div>
