{% extends 'base.html.twig' %}

{% block body %}
    <h3>Жанри</h3>
    <table class="table table-hover">
        <thead>
        <tr>
            <th>Назва</th>
            <th></th>
        </tr>
        </thead>
        <tbody class="table table-hover">
        {% for genre in genres %}
            <tr>
                <td>
                    <a href="{{ path('genre_group',{ 'slug' : genre.slug })}}">{{ genre.name }}</a></td>
                <td>
                    <a href="{{ path('genre_delete_from_bookmark', { 'slug' : genre.slug , 'route' : 'user_cabinet_genre' }) }}">-</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}