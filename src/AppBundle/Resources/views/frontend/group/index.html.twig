{% extends 'base.html.twig' %}

{% block body %}
    <h1>Список Гуртів</h1>
    <table>
        <thead>
        <tr>
            <th>Назва</th>
            <th>Опис</th>
            <th>Рік створення</th>
        </tr>
        </thead>
        <tbody>
        {% for group in groups %}
            <tr>
                <td>{{ group.name }}</td>
                <td>{{ group.description }}</td>
                <td>{{ group.foundedAt|date("m/d/Y") }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}